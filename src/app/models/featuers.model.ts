export class Fet {
    icon: string;
    header: string;
    content: string;

    constructor(icon: string, header: string , content: string){
        this.icon = icon;
        this.header = header;
        this.content = content
    }
}