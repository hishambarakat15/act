export class testemonials {
    icon: string;
    msg: string;
    name: string;

    constructor(icon:string, msg: string,name: string){
        this.icon = icon;
        this.msg = msg;
        this.name = name;
    }
}